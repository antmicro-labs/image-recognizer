image-recognizer
==================

## Dev setup
1. Install  `pipenv` :

```
    $ pip install pipenv
```

2. Create venv and install packages:
```
    $ pipenv install --dev
```

3. Install pre-commit
```
    $ pre-commit install
```
